<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group
        android:id="@+id/menu_top"
        android:checkableBehavior="single"> <!-- This group can remain checkable if other items need it -->
        <item
            android:id="@+id/settingFragment"
            android:icon="@drawable/ic_setting"
            android:title="@string/setting"
            android:checkable="false" /> <!-- Make this item specifically non-checkable -->
    </group>



    <group
        android:id="@+id/menu_bottom"
        android:checkableBehavior="single"> <!-- This group can remain checkable if other items need it -->
        <item
            android:id="@+id/helpFragment"
            android:icon="@drawable/ic_help"
            android:title="@string/help"
            android:checkable="false" /> <!-- Make this item specifically non-checkable -->
    </group>


    <group
        android:id="@+id/menu_logout_or_login"
        android:checkableBehavior="single"> <!-- This group can remain checkable if other items need it -->
        <item
            android:id="@+id/loginFragment"
            android:icon="@drawable/ic_logout"
            android:title="login or logout"
            android:checkable="false" /> <!-- Make this item specifically non-checkable -->
    </group>



</menu>
